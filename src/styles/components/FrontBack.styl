@import '../_variables';

.frontBack
  display: inline-block
  color grey

.front, .back
  font 200 1.5rem 'Dosis'
  cursor pointer
  transition all 0.2s

.active
  font-weight bold
  color blue
  transform scale(1.1)

.separator
  font 200 1.5rem 'Dosis'
  margin 0 1rem
